# Dev dependencies. Mirror of [project.optional-dependencies].dev in
# pyproject.toml. Keep the two in sync.
#
# Install: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8
pytest-cov>=5
hypothesis>=6.100
ruff>=0.6
mypy>=1.11
pre-commit>=3.7
pytest-asyncio>=0.23
httpx>=0.27
pytest-env>=1.1
